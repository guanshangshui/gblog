# Log

## 2026-01-27
- Added optional `voiceUrl` frontmatter for posts.
- Rendered Play/Download controls in the post header on detail pages.
- Standardized audio storage in `public/voice/` with `/voice/*` URLs.
- Added `tests/voice-url.test.mjs` and `pnpm run test:voice`.
- Documented voice usage in `README.md`.
- Added optional `voiceType` for non-MP3 audio and relaxed validation rules.
