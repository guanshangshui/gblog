---
title: "博客图片自动上传：从痛点到解决方案的完整实践"
description: "分享如何用 Python + MinIO 打造自动化图片上传工具，解决多平台内容发布时图片丢失的痛点，实现从本地编辑到一键发布的高效工作流。"
pubDate: "2026-02-05 16:31:53"
category: "技术"
tags: ["Python", "MinIO", "自动化", "博客", "工作流优化"]
banner: "@images/banner/d75f-hwfpcxn2958458.jpg"
date_modified: 2026-02-05 18:33:49
---

## 问题的起点：多平台内容创作的真实困境

我在写博客和各大平台推文时，遇到了一个让人特别头疼的问题。在本地编辑器里精心排版好的文章，图文并茂，看起来很完美。但当你满怀期待地 Ctrl+C、Ctrl+V 复制到微信公众号、知乎或者小红书时，文字过去了，格式也基本保留了，唯独图片全部丢失。

这背后的原因其实很直接：本地图片路径在远程平台上根本无法访问。平台的编辑器只认识它们自己服务器上的图片链接，或者公网可访问的图片 URL。这意味着你必须手动上传每一张图片，一张一张点击上传按钮，等待加载，然后插入到对应位置。如果一篇文章只有一两张图，这个过程还能忍受。但当你写一篇技术教程或者产品评测，需要十几张甚至几十张截图来说明问题时，这个重复劳动就变得极其消耗时间和耐心。

![](https://img.zerodotsix.top/picture-ob/blog/2026/4814f0fbb38409b1742e07f0bff8de806b51db367658af03a1564687aa573230.jpeg)

更让人沮丧的是，这个问题在每个平台上都要重复一遍。你在公众号上传过的图片，到了知乎又得重新上传一次。这种机械重复的工作不仅浪费时间，还会打断创作的思路和节奏。我开始思考，这个问题一定有更优雅的解决方案。

## 探索方向：为什么选择自己动手

面对这个问题，我的第一反应是找找市面上有没有现成的工具。我用 Perplexity 搜索了一圈，确实发现了一些开源项目。这里我想说说为什么我更喜欢用 Perplexity 而不是 ChatGPT 或者 Gemini 来做这类调研。Perplexity 的优势在于它能实时抓取网上最新的信息和项目，给出的结果往往是近期还在活跃维护的。相比之下，ChatGPT 和 Gemini 有时候会推荐 2021 年、2022 年的老项目，点进去一看，仓库已经好几年没更新了，issue 堆积如山或者干脆作者直接宣布不维护开源的了，根本不想用了。

不过即便找到了一些开源方案，我还是决定自己写一个。这个决定有几个原因。首先，我手头正好有 codex ，放着不用感觉有点浪费。其次，我大致评估了一下这个需求的技术复杂度，本质上就是一个文件解析加图片上传的脚本，逻辑并不复杂。最重要的是，自己动手可以完全按照自己的使用习惯来定制功能，后续有新需求也能快速迭代，这种掌控感是用别人的工具体会不到的,最重要的是简单方便一句话就好了。

## 技术选型：从需求到方案的推导过程

确定要自己做之后，我开始和 AI 讨论具体的技术方案。这个过程其实挺有意思，因为它让我更清晰地理解了自己真正需要什么。

我的核心需求可以拆解成三个步骤：第一，扫描 Markdown 文件，找出所有本地图片的引用路径。第二，把这些图片上传到一个公网可访问的存储服务。第三，把 Markdown 文件里的本地路径替换成远程 URL。这三个步骤串起来，就能实现从本地图片到远程链接的自动转换。

![](https://img.zerodotsix.top/picture-ob/blog/2026/34c9c440aa716312a8650ee1e5d26700815263c8abb321ad68df910046fb6ca1.jpeg)

在存储方案的选择上，我最终决定用 MinIO。MinIO 是一个兼容 S3 协议的开源对象存储服务，可以部署在自己的 VPS 上。选择它的原因很实际：首先，它完全免费，不像云服务商的对象存储那样按流量和存储量计费。其次，数据存在自己的服务器上，我对隐私和可控性有更强的掌握感。再者，S3 协议已经是事实上的标准，有大量成熟的 SDK 和工具支持，开发起来很顺畅。

脚本的实现语言我选择了 Python，因为它有丰富的文件处理和网络请求库，写起来简洁高效。整个脚本的核心逻辑大概两三百行代码就能搞定，这也验证了我最初的判断：这个需求的技术复杂度确实不高，关键在于把流程理清楚,搞出提示词就可以直接codex 启动了。

## MinIO 部署：从零到可用的完整流程

在开始写脚本之前，我需要先把 MinIO 部署起来。这个过程比我想象的要简单很多，主要得益于容器化技术的成熟和面板工具的便利性。
![](https://img.zerodotsix.top/picture-ob/blog/2026/35927dfaf76631c7a72a207d95d8cf6ecd87e304ed52cd87f8e1e024a420cc5b.png)
我使用的是宝塔面板来管理我的 VPS，它内置了 Docker 管理功能。在宝塔的应用商店里搜索 MinIO，可以直接一键部署官方镜像容器。整个安装过程不到五分钟，不需要手动配置环境依赖，不需要编译源码，点几下鼠标就完成了。如果你用的是其他面板工具，比如 1Panel、CasaOS 或者 Portainer，应该也都有类似的一键部署功能。MinIO 作为一个热度很高的开源项目，在各大容器管理平台上都有很好的支持。

![](https://img.zerodotsix.top/picture-ob/blog/2026/8f99ee4b4762e1f347bd170e8967e0f65524b9a149cabd2f83f205be571da201.jpeg)

部署完成后，你需要获取 MinIO 的连接信息。在宝塔面板的 Docker 管理界面，找到 MinIO 容器，点击进入详情页面。在"环境变量"（ENV）这个标签页里，你能看到所有的配置参数，包括访问地址、端口号、默认的 Access Key 和 Secret Key。这些信息就是后续脚本连接 MinIO 所需要的凭证。如果你对这些参数的含义不太清楚，直接把整个环境变量列表复制下来，丢给 AI 问一下，它会给你详细解释每个参数的作用。
![](https://img.zerodotsix.top/picture-ob/blog/2026/4ecd73661ccc31aaa4095c42fab85ca01d1029901d0c0ea62844ee2b948dac51.png)

接下来是创建存储桶。登录 MinIO 的 Web 管理界面，创建一个新的 Bucket，这个 Bucket 就是用来存放你上传的图片的。这里有个重要的细节：MinIO 默认创建的 Bucket 是私有的，外部无法直接访问。但我们的需求是要把图片链接分享到公众号、知乎这些平台，所以必须把 Bucket 设置成公开访问模式。

修改 Bucket 权限的方法在 MinIO 的官方文档里有说明，但文档写得比较技术化。我在网上找到了一篇更直白的教程（已经放在文末的参考资料里），按照那篇文章的步骤操作，几分钟就能把 Bucket 改成公开模式。改完之后，你可以随便上传一张图片测试一下，用浏览器直接访问图片的 URL，如果能正常显示，说明配置成功了。

![](https://img.zerodotsix.top/picture-ob/blog/2026/c9c316b4cb01f41a27ff264bdfada1fa2ade78f3ec01244e3a8da36d4e48dd8f.jpeg)

## 实现细节：从脚本到命令行工具的演进

有了可用的 MinIO 服务，接下来就是写脚本了。最初的版本就是一个简单的 Python 脚本，你需要手动指定 Markdown 文件路径，然后运行脚本完成上传和替换。这个版本能用，但每次都要敲一长串命令，还要记住脚本放在哪个目录，用起来不够顺手。

后来我做了一个小优化，把脚本改造成了一个可执行程序，并且把它的路径添加到了系统的环境变量里。这样一来，无论我在哪个目录下工作，只要在命令行里输入脚本名称，就能直接调用。这个改动看起来很小，但实际使用体验提升了一大截。它让这个工具真正融入了我的日常工作流，而不是一个需要特意去找、特意去运行的外部脚本。

![](https://img.zerodotsix.top/picture-ob/blog/2026/9e978157d598a1345a65449f7fd91f6e6ec9c3dc06787b9a51388c332fd549cb.jpeg)

脚本的功能也很灵活，它既可以处理单个 Markdown 文件，也可以处理整个文件夹。当你指定一个文件夹时，它会递归扫描里面所有的 .md 文件，找出所有本地图片引用，批量上传，批量替换。这个批量处理能力在我整理历史文章时特别有用，一次性就能把几十篇文章的图片全部迁移到云端。

配置方面，我把 MinIO 的连接信息，包括服务器地址、存储桶名称、访问密钥这些敏感信息，都放在了配置文件里。还是偷懒,反正也不上传到GitHub上面.

## 意外收获：多平台编辑器的兼容性探索

解决了图片上传的问题后，我以为可以高枕无忧了。但在实际使用中，我又遇到了另一个让人头疼的问题：各大平台的编辑器对 Markdown 的支持程度差异巨大。

微信公众号、知乎、今日头条、小红书，这些内容平台的编辑器几乎都是各自为政。有的支持标准 Markdown 语法，有的只支持富文本，有的甚至用的是自研的编辑器，连基本的md照片语法都不支持。这种碎片化的生态让跨平台发布变得异常痛苦。你在本地用 Markdown 写好的文章，复制到不同平台时，格式经常会乱掉，需要手动调整。

好在我后来在 Obsidian 的插件库里发现了一个开源插件，它能把 Markdown 转换成个平台的格式，并且保留大部分样式。我用自己过往的博客文章测试了一下，发现在微信公众号、知乎、今日头条上都能正常使用，格式基本不会乱。唯一的例外是小红书，它的编辑器似乎用的是完全不同的渲染逻辑，这个插件转换出来的内容在小红书上显示不正常。我猜测小红书可能根本不支持标准的富文本格式，或者它有自己的一套内容审核和渲染机制。这个问题目前还没有完美的解决方案，可能需要针对小红书单独做适配，或者干脆接受它就是个特例。

![](https://img.zerodotsix.top/picture-ob/blog/2026/17c2ee0bf8bf39e3c0bb734ceac664dab6476ec1fa20be7162e49e2318750882.png)

插件叫做 NoteToMP
内容创作工具的碎片化不仅仅是技术问题，背后还有各个平台的商业考量和生态壁垒。每个平台都希望用户在自己的编辑器里创作，这样可以更好地控制内容格式和用户行为。但对于创作者来说，这种割裂的体验真的很糟糕。我更喜欢在 Obsidian 这样的本地编辑器里专注写作，然后一键导出到各个平台，而不是在每个平台的编辑器里重复劳动。

## 意外的拦路虎：HTTPS 混合内容问题

就在我以为一切都搞定，准备享受自动化带来的便利时，现实又给了我一记重锤。我把文章发布到自己部署在 Cloudflare Pages 上的静态博客，满心期待地打开页面，结果发现所有图片都显示不出来。打开浏览器的开发者工具一看，控制台里全是红色的警告信息：Mixed Content，浏览器阻止了 HTTP 资源的加载。

这个问题的根源在于现代浏览器的安全策略。当你的网站使用 HTTPS 协议时，浏览器会阻止页面加载任何 HTTP 协议的资源，包括图片、脚本、样式表等。这是为了防止中间人攻击和数据泄露。而我的 MinIO 服务部署在 VPS 上，访问地址是HTTP + IP + 端口的形式，正好踩中了这个安全限制。

这个问题让我有点措手不及。我的博客必须用 HTTPS，这是 Cloudflare Pages 的默认配置，也是现代网站的标准做法。但 MinIO 服务如果要支持 HTTPS，就需要配置 SSL 证书加证书，而给一个 IP 地址配置新域名又是另一个麻烦事。我开始思考有没有更简单的解决方案。

## 域名代理：优雅地解决协议问题

经过一番研究，我找到了一个既简单又优雅的方案：用nginx反向代理 MinIO 服务。这个思路的核心是，我不直接暴露 MinIO 的 IP 和端口，而是给它套一个域名，然后通过 Nginx 反向代理来转发请求。这样一来，外部访问的是一个 HTTPS 域名，而 Nginx 在内网用 HTTP 访问 MinIO，完美绕过了混合内容的限制。

最初我想过去买一个新域名专门用来做图床。但仔细一算，国内云厂商的域名即便是那种十几个字母的长域名，也要十几块钱一年。如果去 Cloudflare 买，虽然价格也不贵，一年几美元，但还要走一遍注册流程。后来我突然意识到，我自己的博客域名完全可以用二级域名来解决这个问题。比如我的博客是 `example.com`，我可以用 `img.example.com` 或者 `cdn.example.com` 来指向 MinIO 服务。这样不需要额外花钱，也不需要额外的域名管理。
![](Pasted%20image%2020260205183130.png)
下面是二级域名配置步骤.
第一步是 DNS 解析。我的域名托管在 Cloudflare，登录 Cloudflare 的 DNS 管理界面，添加一条 A 记录，主机名填 `img`，指向我的 VPS IP 地址。Cloudflare 的 DNS 解析速度很快，一般几分钟就能生效。如果你开启了 Cloudflare 的 CDN 代理（橙色云朵图标），还能享受免费的全球 CDN 加速和 DDoS 防护，这是国内云厂商很难提供的福利。
1 )![](Pasted%20image%2020260205182257.png)
2)![](Pasted%20image%2020260205182333.png)
3) 配置自己的二级域名的名字和解析地址就好了
4)![](Pasted%20image%2020260205182538.png)
配置过程比我想象的要简单。我用的是宝塔面板，它的 Nginx 管理功能做得很直观。在宝塔面板里添加一个新站点，域名填 img.example.com，然后在反向代理设置里填上 MinIO 的内网地址和端口号，比如 http://127.0.0.1:19000。
保存配置后，宝塔会自动生成 Nginx 配置文件并重载服务。
5)![](Pasted%20image%2020260205182643.png)

最后一步是申请 SSL 证书。宝塔面板集成了 Let's Encrypt 的自动证书申请功能，点几下鼠标就能给 `img.example.com` 配置好 HTTPS。证书会自动续期，不需要手动维护。配置完成后，我用浏览器访问 `https://img.example.com/bucket-name/test.jpg`，图片正常显示，而且地址栏显示的是安全的 HTTPS 小锁图标。
![](Pasted%20image%2020260205182820.png)

这个过程中让我有点自豪的是，我基本上是独立完成的，没有去翻各种教程博客。自己也当了会运维, 哈哈哈.

配置好域名代理后，我需要修改一下脚本，把生成的图片链接从 `http://xxx:xxxbucket/image.jpg` 改成 `https://img.example.com/bucket/image.jpg`。这只是一个简单的字符串替换，改完之后重新运行脚本，所有新上传的图片都会使用新的 HTTPS 域名。对于历史文章里的旧链接，我写了一个简单的批量替换脚本，一次性把所有文章里的 HTTP 链接都改成了 HTTPS。

## 工作流的最终形态：效率与掌控感的平衡

经过这一系列折腾，我现在的内容发布工作流终于真正成熟了。我在 Obsidian 里用 Markdown 写作，插入本地图片。写完之后，运行一下脚本，所有本地图片自动上传并转换成 HTTPS 远程链接。然后用 Obsidian 的插件把 Markdown 转换成富文本，复制粘贴到各个平台。整个过程下来，从写作完成到发布，基本上可以在三分钟内完成，而且不需要手动上传任何图片。

![](https://img.zerodotsix.top/picture-ob/blog/2026/f350b626df3a1463891fc361c766c852f2bd3d3d93818658373301e25c354439.jpeg)

当然，市面上也有一些一键发布的平台，可以同时把内容推送到多个渠道。但这类服务大多是收费的，而且你需要把账号授权给第三方，我对这种方式不太放心。相比之下，我更喜欢现在这种方式：工具都在自己手里，数据存在自己的服务器上，整个流程透明可控。虽然需要多点几次鼠标，但这点额外的操作换来的是对内容和数据的完全掌控，我觉得很值得。

## 开放与分享：工具的价值在于被使用

这个脚本的代码我已经整理好了，后续会分享出来。需要说明的是，我分享的是最基础的脚本版本，它包含了核心的图片扫描、上传和替换逻辑，但没有把环境变量配置、命令行参数解析这些锦上添花的功能打包进去。这样做的原因是，每个人的使用场景和偏好都不一样，有人喜欢用配置文件，有人喜欢用环境变量，有人可能想加上图片压缩或者格式转换的功能。

如果你想要更完善的命令行工具体验，可以把这篇文章和我的脚本代码一起丢给 AI，让它帮你生成一个定制版本。现在的 AI 编程助手在这种明确需求的改造任务上已经非常成熟了，你只需要清楚地描述你想要什么功能，它就能给出可用的代码。比如你可以说："帮我把这个脚本改造成命令行工具，支持通过环境变量读取 MinIO 配置，支持批量处理文件夹，并且添加进度显示。"AI 会理解你的需求，结合原有代码，生成符合你期望的版本。

我分享这个经历，不是为了炫耀技术，而是想说明一个观点：很多看起来需要忍受的不便，其实都可以通过一点点动手能力来改善。你不需要是专业程序员，也不需要从零学习编程语言。借助 AI 工具，普通人也能快速实现自己的想法，打造适合自己的工作流。这种从发现问题到解决问题的过程，带来的成就感和掌控感，远比单纯使用现成工具要强烈得多。

技术的本质是为人服务的，工具应该适应你的习惯，而不是让你去适应工具。当你发现某个流程很繁琐时，不妨停下来想一想，这个问题能不能用更聪明的方式解决。很多时候，答案就在你手边的 AI 助手里。

---

## 参考资料

- [MinIO 社区版修改桶权限设置](https://blog.csdn.net/lhq15222807611/article/details/151361609)

注:最后发现自己的ob配置比较复杂导致脚本不具备通用性所以就不上传了,各位想要尝试的自己通过下面的提示词让AI写一份就好了,高度自定义.

提示词:

```C++
你是一个资深 Python 工程师，请为“Markdown 本地图片自动上传 MinIO 并替换链接”实现一个可用的命令行工具。

背景/目标：
- 我写博客（Markdown），文章里有很多本地图片引用，不想手动逐张上传。
- 我使用 MinIO 作为云端对象存储（S3 compatible）。
- 我希望只运行一条命令，脚本就能：自动扫描 Markdown -> 找到本地图片文件 -> 批量上传到 MinIO -> 把 Markdown 里的本地图片链接替换成云端图片直链。
- 我希望后续更换访问域名/URL 规则时，只需要改一个 base_url 或配置参数即可。

必须交付：
1) 一个 Python CLI 脚本（例如 md_minio_img.py），支持：
   - --input：单个 .md 文件或目录（目录递归处理所有 .md）
   - --endpoint, --access-key, --secret-key, --secure(http/https)
   - --bucket：MinIO bucket
   - --prefix：上传对象前缀（可选，如 blog/2026/）
   - --base-url：生成直链用的 URL 前缀（例如 https://img.example.com 或 https://minio.example.com）
   - --dry-run：不写回文件，只输出将发生的上传与替换
   - --backup：默认开启，写回前生成 .bak
   - --extensions：允许的图片扩展名列表（默认 png,jpg,jpeg,gif,webp,svg）

2) 行为要求：
   - 只替换本地图片链接：识别 ![alt](path) 中的 path，若是 http/https/data: 则跳过。
   - 相对路径必须以“Markdown 文件所在目录”为基准解析成真实文件路径。
   - 找不到文件要给出清晰报错（但不要让整个批处理崩掉；继续处理其它文件并在最后汇总）。
   - 去重：同一个图片文件只上传一次。建议：
       a) 以图片内容 hash (sha256) 生成 object_key（保留原扩展名），或
       b) 以“文件绝对路径 + mtime + size”的组合做缓存。
   - 上传后生成云端 URL：url = base_url + "/" + bucket + "/" + object_key（或根据参数提供模板）。
   - 替换 Markdown：把 ![alt](本地path) 的 path 替换成云端 url，alt 文本保持不变。
   - 保持 Markdown 其它内容不变（尽量少改动格式）。

3) MinIO 连接与上传：
   - 使用官方 MinIO Python SDK（pip install minio）。
   - 若 bucket 不存在，可提供选项自动创建（默认不创建也可以，但要提示）。

4) 输出与可用性：
   - 运行时打印进度（处理了多少 md、上传了多少图、替换了多少链接、失败多少）。
   - 代码结构清晰：解析参数、扫描文件、解析图片引用、上传、替换写回 分模块函数实现。
   - 给出 README 风格的使用示例（命令行示例 + 运行前需要的配置/环境变量示例）。

额外加分（可选）：
- 支持 Markdown reference-style 图片（形如 ![alt][id] + [id]: ./img.png）。
- 支持并发上传（线程池），但要注意重复上传与日志。
- 支持忽略目录（如 .git, node_modules, dist）。

请输出：
- 完整代码（可直接复制运行）
- 简短说明：如何安装依赖、如何配置参数、如何运行、注意事项
```

各大平台的效果图:
微信的适配不错
![](https://img.zerodotsix.top/picture-ob/blog/2026/856fa174cc60f901e28e2a79d66569c9713149dbf95aff0dfcb98f333baedcc1.png)
知乎ok
![](https://img.zerodotsix.top/picture-ob/blog/2026/d0f0449e134dcc4cb0b7138d0ef92a7bdf7ad05893b6bfa46e2ffa1b3dce3f42.png)
头条也不错
![](https://img.zerodotsix.top/picture-ob/blog/2026/46d87a09bcb6187b71de8bda49fe8b8e9c9d2a5b8881992cf7f29ac057bafaa0.png)

小红书不支持这种照片语法.

 